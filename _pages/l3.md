---
theme: just-the-docs
layout: default
data: l3
title: 🤹🏿‍♂️ Learning
permalink: /l3/
isHome: false
nav_order: 5
isPost: false
---
<link rel="stylesheet" href="{{ '/assets/css/custom.css' | relative_url }}?v=20260822-compact-quiz">

{% assign learning_posts = site.posts | where_exp: "post", "post.categories contains 'l3'" %}

<div class="qanda-page l3-page">
  <header class="qanda-hero">
    <p class="qanda-eyebrow">Life long learning</p>
    <h1>Learning</h1>
    <p class="qanda-summary">Browse short learning notes by domain, track, and framework, or search for a specific concept or library.</p>
  </header>

  <div class="qanda-controls" aria-label="Learning filters">
    <label class="qanda-search" for="l3-search">
      <span>Search notes</span>
      <input id="l3-search" type="search" placeholder="Search TensorFlow, transformers, regression..." autocomplete="off">
    </label>

    <div id="l3-topic-filters" class="qanda-topic-filters l3-topic-filters" aria-label="Learning topic filters">
      <span class="l3-topic-filter-label">Browse topics</span>
      <button type="button" class="qanda-filter is-active" data-topic="all" aria-pressed="true">All notes</button>
      {% assign unique_topics = "" %}
      {% for post in learning_posts %}
        {% if post.topic %}
          {% unless unique_topics contains post.topic %}
            <button type="button" class="qanda-filter" data-topic="{{ post.topic }}" aria-pressed="false">{{ post.topic }}</button>
            {% assign unique_topics = unique_topics | append: post.topic | append: "," %}
          {% endunless %}
        {% endif %}
      {% endfor %}
    </div>
  </div>

  {% if learning_posts.size > 0 %}
    {% assign unique_domains = "" %}
    {% for post in learning_posts %}
      {% if post.domain %}
        {% unless unique_domains contains post.domain %}
          <section class="qanda-section l3-section" id="{{ post.domain | slugify }}-notes" aria-labelledby="{{ post.domain | slugify }}-title">
            <div class="qanda-section-header">
              <p class="qanda-section-kicker">Domain</p>
              <h2 id="{{ post.domain | slugify }}-title">{{ post.domain }}</h2>
            </div>

            {% assign displayed_groups = "" %}
            {% for grouped_post in learning_posts %}
              {% if grouped_post.domain == post.domain %}
                {% capture group_key %}{{ grouped_post.track }}|{{ grouped_post.subtrack }}{% endcapture %}
                {% unless displayed_groups contains group_key %}
                  <h3 class="topic-header learning-topic-header" data-topic="{{ grouped_post.topic }}" data-group="{{ group_key }}">{{ grouped_post.track }} / {{ grouped_post.subtrack }}</h3>
                  {% assign displayed_groups = displayed_groups | append: group_key | append: "," %}
                {% endunless %}

                {% unless grouped_post.quiz or grouped_post.quiz_collection %}
                  <div class="question-entry learning-entry{% if grouped_post.practice_url %} learning-entry--with-practice{% endif %}" data-topic="{{ grouped_post.topic }}" data-domain="{{ grouped_post.domain }}" data-track="{{ grouped_post.track }}" data-subtrack="{{ grouped_post.subtrack }}" data-group="{{ group_key }}" data-title="{{ grouped_post.title | escape }}" data-body="{{ grouped_post.content | strip_html | strip_newlines | escape }}">
                    {% if grouped_post.practice_url %}
                      <a class="learning-practice-icon" href="{{ grouped_post.practice_url | relative_url }}" target="_blank" rel="noopener" aria-label="Open AZ-500 practice questions in a new tab" title="Start practice questions">▶</a>
                    {% endif %}
                    <button type="button" class="collapsible">
                      <span class="collapsible-content-header">
                        <strong>{{ grouped_post.title }}</strong><br>
                        <em>{{ grouped_post.topic }}</em>
                      </span>
                    </button>
                    <div class="collapsible-content">
                      <div class="answer-entry">
                        <p class="learning-entry-meta">
                          <span>{{ grouped_post.domain }}</span>
                          <span>{{ grouped_post.track }}</span>
                          <span>{{ grouped_post.subtrack }}</span>
                        </p>
                        {{ grouped_post.content | markdownify }}
                      </div>
                    </div>
                  </div>
                {% endunless %}
              {% endif %}
            {% endfor %}
          </section>
          {% assign unique_domains = unique_domains | append: post.domain | append: "," %}
        {% endunless %}
      {% endif %}
    {% endfor %}

    <p class="qanda-empty" id="l3-empty">Select topics to continue.</p>
  {% else %}
    <section class="am-card am-empty-state">
      <h2>Coming Soon</h2>
      <p>Learning notes are being prepared.</p>
    </section>
  {% endif %}
</div>

{% include footer.md %}
