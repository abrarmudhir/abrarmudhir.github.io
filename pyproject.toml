[project]
name = "abrarmudhir-github-io"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.black]
target-version = ["py313"]

[dependency-groups]
dev = [
    "black>=26.5.1",
    "pre-commit>=4.6.2",
]
